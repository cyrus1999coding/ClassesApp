# Properties - Autogenerated - to protect our member variable  

We're going to look at 🔑`Properties`, they are important aspect of  
🔑`Encapsulation` which is an important part of OOP .  

What `Properties` does is it `Shields variables from outside Classes` .  

We're already shielding variables from outside Classes ↓  

```cs
Program.cs

namespace ClassesApp
{
    internal class Program
    {
        static void Main(string[] args)
        {
            Car audi = new Car("A3","german");
            Car bmw = new Car("I7","austulia");
            

            Console.WriteLine("Hello, World!");

            Console.ReadKey();
        }
    }
}
```

```cs
// Car.cs
using System;
using System.Collections.Generic;
using System.Text;

/// <summary>
/// Represents a car with a specific model.
/// </summary>
namespace ClassesApp
{
    internal class Car
    {
        // member variable
        private string _model = ""; 👈 
        private string _brand = "";


        /// <summary>
        /// Initializes a new instance of the <see cref="Car"/> class.
        /// </summary>
        /// <param name="model">The model name of the car.</param>
        public Car(string model, string brand)
        {
            _model = model;
            _brand = brand;
            Console.WriteLine("A car of the model " + _model + " With brand of " + _brand + " has been created Successfully");
        }

    }
}
```

This is not enough we also want to hace control over how this variable  
`_model` can be 🔑`accessed` from the outside and how it can be 🔑`changed`(Modified) .  
Becasue right now ew can not neither `Access` it nor `Modify` it from the outside .

```
namespace ClassesApp
{
    internal class Program
    {
        static void Main(string[] args)
        {
            Car audi = new Car("A3","german");
            Car bmw = new Car("I7","austulia");

            audi._model = ""; ❌

            Console.WriteLine("Hello, World!");

            Console.ReadKey();
        }
    }
}

```
- ❌ : 'Car._model' is inaccessible due to its 🔑`protection level`  

🔑`protection level` : Currently the protection level is `private` ↓  

```cs
Car.cs

private string _model = "";
```
- So this `private` **Keyword** is setting the `protection level` .
- `private` : Means that his variable `_model` can only be modified  
  🔑`Inside` of the same Class

If we change that to `public` ↓  

```cs
// Car.cs

using System;
using System.Collections.Generic;
using System.Text;

/// <summary>
/// Represents a car with a specific model.
/// </summary>
namespace ClassesApp
{
    internal class Car
    {
        // member variable
        public string _model = ""; 👈
        private string _brand = "";


        /// <summary>
        /// Initializes a new instance of the <see cref="Car"/> class.
        /// </summary>
        /// <param name="model">The model name of the car.</param>
        public Car(string model, string brand)
        {
            _model = model;
            _brand = brand;
            Console.WriteLine("A car of the model " + _model + " With brand of " + _brand + " has been created Successfully");
        }

    }
}
```

```
// Program.cs
namespace ClassesApp
{
    internal class Program
    {
        static void Main(string[] args)
        {
            Car audi = new Car("A3","german");
            Car bmw = new Car("I7","austulia");

            audi._model = "";

            Console.WriteLine("Hello, World!");

            Console.ReadKey();
        }
    }
}

```
- 🛠✅ : The Error disapears .  
  Because now we can `Modify` it because of the `Protection Level` is not `private` anymore .  
  Now out side of the Class like `Program.cs` Class have `Access` to it .
  
- ⛔ :  
  This is very bad we shouldnot do this, The idea behind `Encapsulation` and in general OOP  
  is that we have cleaner way of modifying it, Why is that ❔  
  💡 :  
  Because we need to able to define 🔑`When` and 🔑`How` the `_model` variable can be Accessed .  
  We can create a surrounding 🔑`Property` for it, So how would we do that ❔  
  💡 :  
  We can use something called 🔑`Property` .


```cs
using System;
using System.Collections.Generic;
using System.Text;

/// <summary>
/// Represents a car with a specific model.
/// </summary>
namespace ClassesApp
{
    internal class Car
    {
        // member variable
        private string _model = "";
        private string _brand = "";

        public int Model { get; set; }

        /// <summary>
        /// Initializes a new instance of the <see cref="Car"/> class.
        /// </summary>
        /// <param name="model">The model name of the car.</param>
        public Car(string model, string brand)
        {
            _model = model;
            _brand = brand;
            Console.WriteLine("A car of the model " + _model + " With brand of " + _brand + " has been created Successfully");
        }

    }
}

```
- `public int Model { get; set; }` :  
  🔑 `Property` start with Capital Letter .  
  Sp this `Model` is a `Property` .

- `get; set;` :  
  Means that we can `Modify` how this **property** can be `got` and we can modify how it can be `set` ( Overwritten ) .

There are multiple ways that this →` private string _model = "";`  
Can be `surrounded` or `protected` by a `Property` .

`Quick actions and refactorings` → `Encapsulate Field:'_model' (and use property)` ↓   

```cs
// Car.cs

using System;
using System.Collections.Generic;
using System.Text;

/// <summary>
/// Represents a car with a specific model.
/// </summary>
namespace ClassesApp
{
    internal class Car
    {
        // member variable
        private string _model = "";
        private string _brand = "";

        // property
        public int Model { get; set; }
        public string Model1 { get => _model; set => _model = value; } 👈

        /// <summary>
        /// Initializes a new instance of the <see cref="Car"/> class.
        /// </summary>
        /// <param name="model">The model name of the car.</param>
        public Car(string model, string brand)
        {
            Model1 = model; 👈
            _brand = brand;
            Console.WriteLine("A car of the model " + Model1 👈 + " With brand of " + _brand + " has been created Successfully");
        }

    }
}

```
- `public string Model1 { get => _model; set => _model = value; }` :  
  It would create this code here .  
  This code uses something called 🔑`Lambda Expressions` , Which we cover later .  
  Basically it says it whenever we want to `get` the `_model`, we can `Access` it .  
  Whenever we want to set the `_model` whatever value you give me i'm going to set that as the `_model` .

- 🔑 We can see also our code Automaticallt protected our → `private string _model = "";`  
  In the Constructor to :  
  ```cs
        public Car(string model, string brand)
        {
            Model1 = model; 👈
            _brand = brand;
            Console.WriteLine("A car of the model " + Model1 👈 + " With brand of " + _brand + " has been created Successfully");
        }
  ```
  So it OverWrote our `_model`, This is because it's part of `Encapsulation` of making sure that our `variable` is `Encapsulated` and  
  You have full conrtoll over how it is 🔑`set` and 🔑`gotten` . ↓  
  ```cs
  public string Model1 { get => _model; set => _model = value; } 
  ```

We can do the same thing with our `_brand` :

```cs
using System;
using System.Collections.Generic;
using System.Text;

/// <summary>
/// Represents a car with a specific model.
/// </summary>
namespace ClassesApp
{
    internal class Car
    {
        // member variable
        private string _model = "";
        private string _brand = ""; 👈

        // property
        public string Model1 { get => _model; set => _model = value; }
        public string Brand { get => _brand; set => _brand = value; } 👈

        /// <summary>
        /// Initializes a new instance of the <see cref="Car"/> class.
        /// </summary>
        /// <param name="model">The model name of the car.</param>
        public Car(string model, string brand)
        {
            Model1 = model;
            Brand = brand; 👈
            Console.WriteLine("A car of the model " + Model1 + " With brand of " + Brand 👈 + " has been created Successfully");
        }

    }
}
```
- `Brand = brand;` :  
  🔑 Here in the `constructor`, it say :  
    - ⛔ you dont have `Access` to this `private member variable` → `private string _brand = "";`  
    - ✅ The only `person`/`code` that has `Access` to it, is the `Proterty` itself → `Brand`  
      So only the `Proterty` `Brand` can now Modify `member variable` `_brand` .  
      Nowhere else should we `Modify` `_brand` in our code .  
      🔑 If we want to `Modify` `_brand` we would need to go through this `Protector` → `Proterty` `Brand` .  
      Because `Brand` defines who can get in and who can not get in .  
      👀 ) For example we can say that only if you give me a **value** that is not `null`, we're going to Accept it .  
      👀 ) Or only if you give the value that is Acceptable `string` ( has a length of 10 characters maximum ) only then we'll accept you Entry   
      